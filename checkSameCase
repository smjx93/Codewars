// Write a function that will check if two given characters are the same case.

// If either of the characters is not a letter, return -1
// If both characters are the same case, return 1
// If both characters are letters, but not the same case, return 0

// Examples
// 'a' and 'g' returns 1

// 'A' and 'C' returns 1

// 'b' and 'G' returns 0

// 'B' and 'g' returns 0

// '0' and '?' returns -1 


function sameCase(a, b){
    if(a.toUpperCase() === a.toLowerCase() || b.toLowerCase() === b.toUpperCase()){
       return -1
     }else if(a === a.toLowerCase() && b === b.toLowerCase() || a === a.toUpperCase() && b === b.toUpperCase()){
         return 1
     }else{
       return 0
     }
   }



// The sameCase function takes two arguments a and b, which are expected to be characters. 
// The function then checks whether either of the characters is not a letter 
// (i.e., not uppercase or lowercase) using the toUpperCase() and toLowerCase() methods. 

// If either character is not a letter, the function returns -1.

// If both characters are letters, the function then checks whether they are of the 
// same case by comparing their uppercase and lowercase versions. If both characters are 
// the same case (i.e., both uppercase or both lowercase), the function returns 1. 
// If the characters are different cases, the function returns 0.
   
// Overall, the function checks whether two given characters are the same case, 
// returns 1 if they are, returns 0 if they are different cases but both letters, and 
// returns -1 if either of the characters is not a letter.